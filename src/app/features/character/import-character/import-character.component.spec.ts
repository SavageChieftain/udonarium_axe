import { ImportCharacterComponent } from '@axe/features/character/import-character/import-character.component';

describe('ImportCharacterComponent', () => {
  it('should be defined', () => {
    expect(ImportCharacterComponent).toBeDefined();
  });
});
