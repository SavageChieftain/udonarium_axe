import { ChatMessageSettingComponent } from './chat-message-setting.component';

describe('ChatMessageSettingComponent', () => {
  it('should be defined', () => {
    expect(ChatMessageSettingComponent).toBeDefined();
  });
});
