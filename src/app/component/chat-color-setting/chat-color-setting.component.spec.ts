import { ChatColorSettingComponent } from './chat-color-setting.component';

describe('ChatColorSettingComponent', () => {
  it('should be defined', () => {
    expect(ChatColorSettingComponent).toBeDefined();
  });
});
