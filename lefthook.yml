commit-msg:
  jobs:
    - name: commitlint
      run: npx commitlint --edit {1}

pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: '*.{ts,html}'
      run: npx ng lint

    - name: test
      run: npx ng test

pre-push:
  jobs:
    - name: build
      run: npm run build
