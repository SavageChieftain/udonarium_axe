{
  "name": "udonarium-axe-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Udonarium Axe 利用ガイド (VitePress)",
  "scripts": {
    "sync:icons": "node scripts/sync-app-icons.mjs",
    "predev": "npm run sync:icons",
    "dev": "vitepress dev",
    "prebuild": "npm run sync:icons",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "2.0.0-alpha.19"
  }
}
