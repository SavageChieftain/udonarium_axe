# オブジェクトの基本操作

テーブル上のオブジェクト（コマ・カード・ダイス・メモなど）に共通する操作です。

## 移動・回転

- **移動** — オブジェクトをドラッグ
- **回転** — オブジェクトにマウスを乗せると現れる回転ハンドルをドラッグ（2D 表示中は無効）

## 高度（高さ）

一部のオブジェクトは、右クリックメニューの **「高度設定」** で高さを変えて積み重ねられます。
「高度を 0 にする」で地面に戻せます。

## 複数選択・一括操作

- `Ctrl + クリック` で複数のオブジェクトを選択
- 選択した複数をまとめてドラッグ・複製・移動
- 何もない場所を長押し（ドラッグ）で矩形（マーキー）選択

## コンテキストメニュー

各オブジェクトを**右クリック**すると、対象専用のメニュー（コピー・削除・詳細表示など）が開きます。
同じ位置にオブジェクトが重なっている場合は、メニューから対象を選び直せます。

個別の操作は各ページを参照してください: [キャラクターコマ](/manual/character)・[カード](/manual/cards)・[ダイス](/manual/dice)・[共有メモ](/manual/notes)・[射程範囲](/manual/range)・[地形](/manual/terrain)・[マップマスク](/manual/map-mask)。
